package moe.sourcekaos.senkohub.references;

/**
 * Contains the available permissions needed to
 * execute commands, bypass restrictions, and so on
 * */
public abstract class PermissionTypes {
    // Abilities
    public static final String SET_SPAWN = "senkohub.set-spawn";

    // Bypasses
    public static final String JOIN_BYPASS = "senkohub.join.bypass";
    public static final String INVENTORY_BYPASS = "senkohub.inventory.bypass";
    public static final String INTERACTION_BYPASS = "senkohub.interaction.bypass";
    public static final String LANDSCAPE_BYPASS = "senkohub.landcape.bypass";
}
